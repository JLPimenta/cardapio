{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm i && npx prisma generate && npx prisma migrate deploy && npx prisma db seed && npm run build",
  "builds": [
    {
      "src": "./dist/src/main",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./dist/src/main"
    }
  ]
}
