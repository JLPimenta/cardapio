{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm i && npx prisma generate && npx prisma migrate deploy && npx prisma db seed && npm run build && npm run start:dev",
  "builds": [
    {
      "src": "./src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
    }
  ]
}
